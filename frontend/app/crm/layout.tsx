import type { ReactNode } from "react";

// TODO: proteger esta rota quando a autenticacao real for implementada.
export default function CrmRootLayout({ children }: { children: ReactNode }) {
  return children;
}

